{
  "name": "polymarket-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only Polymarket market discovery, book metrics, and signal features.",
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/index.ts",
    "test": "tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
